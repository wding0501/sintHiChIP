% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sinthichip_utils.R
\name{sintHiChIP_create_granges_fast}
\alias{sintHiChIP_create_granges_fast}
\title{Create GRanges with data.table compatibility}
\usage{
sintHiChIP_create_granges_fast(
  df,
  seqnames.field = "seqnames",
  start.field = "start",
  end.field = "end"
)
}
\arguments{
\item{df}{Data.frame or data.table. Data frame or data.table with genomic coordinates}

\item{seqnames.field}{Character. Column name for chromosome. Default "seqnames"}

\item{start.field}{Character. Column name for start position. Default "start"}

\item{end.field}{Character. Column name for end position. Default "end"}
}
\value{
GRanges object with genomic coordinates and metadata
}
\description{
Helper function that converts data.frame or data.table to GRanges object
while preserving additional metadata columns.
}
\examples{
\dontrun{
# Basic usage with default column names
df <- data.frame(
  seqnames = c("chr1", "chr2"), 
  start = c(1000, 2000), 
  end = c(1500, 2500),
  score = c(10, 20)
)
gr <- sintHiChIP_create_granges_fast(df)

# Custom column names
df2 <- data.frame(
  chromosome = c("chr1", "chr2"),
  pos_start = c(1000, 2000),
  pos_end = c(1500, 2500)
)
gr2 <- sintHiChIP_create_granges_fast(df2, 
                                      seqnames.field = "chromosome",
                                      start.field = "pos_start", 
                                      end.field = "pos_end")

# With data.table input
library(data.table)
dt <- data.table(seqnames = "chr1", start = 1000, end = 2000, value = 5)
gr3 <- sintHiChIP_create_granges_fast(dt)
}

}
