% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_sintHiChIP.R
\name{run_sintHiChIP}
\alias{run_sintHiChIP}
\title{Main sintHiChIP Pipeline}
\usage{
run_sintHiChIP(
  mode,
  outdir,
  hicpro_output = NULL,
  sample_name = NULL,
  peaks,
  resfrags = NULL,
  chr_size = NULL,
  build_matrix = NULL,
  bin_size = 5000,
  normSiteFile,
  FDR = 0.01,
  min_dist = 20000,
  max_dist = 2e+06,
  half_length = 73,
  no_merge = FALSE,
  max_anchor_width = 50000,
  keep_temp = FALSE,
  nbins = 10,
  peak_pad = 500,
  merge_gap = 500,
  make_washu = TRUE
)
}
\arguments{
\item{mode}{Character string. Processing mode: "local" or "global"}

\item{outdir}{Character string. Output directory for results}

\item{hicpro_output}{Character string. Path to HiC-Pro output directory (contains allValidPairs)}

\item{sample_name}{Character string. Sample identifier for file naming (optional, will auto-detect if NULL)}

\item{peaks}{Character string. Path to peak file (BED format)}

\item{resfrags}{Character string. Path to restriction fragments file (Local mode)}

\item{chr_size}{Character string. Path to chromosome sizes file (Global mode)}

\item{build_matrix}{Character string. Path to build_matrix tool (Global mode)}

\item{bin_size}{Numeric. Genomic bin size for matrix generation (Global mode, default: 5000)}

\item{normSiteFile}{Character string. Path to normalization site density file}

\item{FDR}{Numeric. False Discovery Rate threshold (default: 0.01)}

\item{min_dist}{Numeric. Minimum interaction distance in base pairs (default: 20000)}

\item{max_dist}{Numeric. Maximum interaction distance in base pairs (default: 2000000)}

\item{half_length}{Numeric. Read extension length (default: 73)}

\item{no_merge}{Logical. Skip anchor merging in Local mode (default: FALSE)}

\item{max_anchor_width}{Numeric. Maximum anchor width in Local mode (default: 50000)}

\item{keep_temp}{Logical. Keep temporary files (default: FALSE)}

\item{nbins}{Numeric. Number of bins for statistical modeling (default: 10)}

\item{peak_pad}{Numeric. Peak padding in bp (default: 500)}

\item{merge_gap}{Numeric. Merge gap for bedtools merge (default: 500)}

\item{make_washu}{Logical. Create WashU/UCSC compatible files (default: TRUE)}
}
\value{
Invisible NULL. Results written to output directory
}
\description{
Main entry point for sintHiChIP processing, supporting both Local and Global modes.
This function properly delegates to the specialized functions with unified parameter handling.
}
\examples{
\dontrun{
# Local Mode
run_sintHiChIP(
  mode = "local",
  outdir = "/path/to/output",
  hicpro_output = "/path/to/hicpro_results",
  sample_name = "sample1",
  peaks = "/path/to/peaks.bed",
  resfrags = "/path/to/restriction_fragments.bed",
  normSiteFile = "/path/to/norm_sites.bed"
)

# Global Mode
run_sintHiChIP(
  mode = "global",
  outdir = "/path/to/output",
  hicpro_output = "/path/to/hicpro_results",
  sample_name = "sample1",
  peaks = "/path/to/peaks.bed",
  chr_size = "/path/to/chrom_sizes.txt",
  build_matrix = "/path/to/build_matrix",
  normSiteFile = "/path/to/norm_sites.bed"
)
}

}
